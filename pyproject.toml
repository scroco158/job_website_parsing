[tool.poetry]
name = "job-website-parsing"
version = "0.1.0"
description = ""
authors = ["scroco158 <scroco@mail.ru>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
requests = "^2.31.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
